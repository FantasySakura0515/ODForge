__pycache__/
*.pyc
.venv/
venv/
env/
node_modules/
dist/
build/
*.egg-info/
.DS_Store

# Secrets — NEVER commit (repo is public). DEEPSEEK_API_KEY etc. live here for local dev.
.env
.env.*
!.env.example

.superpowers/
報名文件/
佐證資料/
競賽研究/
odforge/.venv/
odforge/**/__pycache__/
odforge/*.egg-info/
上傳區/
.~lock.*#

# Agent/tooling scratch (binary decks, preview PNGs, skill scratch files)
.jez/
.impeccable.md

# Agent scratch that keeps reappearing at the repo root as untracked noise —
# ignored by name so `git status` stays a signal about the project.
odforge/preview-*.png
.claude/worktrees/
